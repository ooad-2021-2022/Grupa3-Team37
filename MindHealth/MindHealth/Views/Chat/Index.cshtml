@model IEnumerable<MindHealth.Models.Chat>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Chat</h1>

<p>      @if(@Model.Any()){
              <td> <input name="New message" value="New message" id="Button1" title="New message" style="border-style:Solid; color:#FFFFFF; background-color:#193f91" type="submit" onclick="location.href='@Url.Action("Create","Chat",new{@idUsera=@Model.First().idUser})'"> </td>
}</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.idUser)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.idTherapist)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.message)
            </th>
        <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.idUser)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.idTherapist)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.message)
            </td>
          
        </tr>
}
    </tbody>
</table>
